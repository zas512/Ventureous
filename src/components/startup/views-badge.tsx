/**
 * Server component that increments the view count on render.
 * In static/demo mode this is a no-op and renders nothing.
 */
export function ViewsBadge({ id: _id }: { id: string }) {

  return null;
}
